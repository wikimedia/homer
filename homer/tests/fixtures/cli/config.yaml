base_paths:
  public: homer/tests/fixtures/public
  private: homer/tests/fixtures/private

transports:
  username: user
  ssh_config: ~/.ssh/config.homer

selectors:
  renderer:
    python:
      - manufacturer: nokia
  transport:
    json_rpc:
      - manufacturer: nokia
